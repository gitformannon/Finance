import 'package:flutter/material.dart';

import 'app_constants.dart';

class EasyLocale {
  static final all = [
    const Locale(AppConstants.uz),
    const Locale(AppConstants.ru),
    const Locale(AppConstants.en),
  ];
}
