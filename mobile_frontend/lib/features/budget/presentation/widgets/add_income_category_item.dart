import 'package:flutter/material.dart';

class AddIncomeCategoryItem extends StatelessWidget {
  const AddIncomeCategoryItem({
    super.key

  });

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        color: Colors.red
      ),
    );
  }
}
